CREATE TABLE account_details
(
id             INTEGER,
firstName      VARCHAR(100),
lastName       VARCHAR(100),
zipCode        INTEGER,
city           VARCHAR(100),
color          VARCHAR(100),
PRIMARY KEY (id)
);